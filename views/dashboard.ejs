<!DOCTYPE html>
<html>

<head>
    <title>Dashboard</title>
    <link rel="stylesheet" href="common.css">
</head>
<body>
    <h1>Dashboard</h1>
    <% username = 'Admin' %>
    <% if (typeof(username) !== 'undefined'){ %>
        <p>Logged in as: </p><p class="ul"><%= username %></p>
        <p><a href="/login">Log Out</a></p>
    <% } else { %>
        <p>Not logged in.</p>
    <% } %>

</body>

</html>